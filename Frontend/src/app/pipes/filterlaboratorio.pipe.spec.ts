import { FilterlaboratorioPipe } from './filterlaboratorio.pipe';

describe('FilterlaboratorioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterlaboratorioPipe();
    expect(pipe).toBeTruthy();
  });
});
