import { Filter_orden_entregaPipe } from './filtro-orden-entrega.pipe';

describe('FiltroOrdenEntregaPipe', () => {
  it('create an instance', () => {
    const pipe = new Filter_orden_entregaPipe();
    expect(pipe).toBeTruthy();
  });
});
