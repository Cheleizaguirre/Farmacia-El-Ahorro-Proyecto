import { FilterclientePipe } from './filtercliente.pipe';

describe('FilterclientePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterclientePipe();
    expect(pipe).toBeTruthy();
  });
});
