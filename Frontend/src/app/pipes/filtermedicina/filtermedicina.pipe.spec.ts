import { FiltermedicinaPipe } from './filtermedicina.pipe';

describe('FiltermedicinaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltermedicinaPipe();
    expect(pipe).toBeTruthy();
  });
});
