import { FilterfacturaPipe } from './filterfactura.pipe';

describe('FilterfacturaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterfacturaPipe();
    expect(pipe).toBeTruthy();
  });
});
